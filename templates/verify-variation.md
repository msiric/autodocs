IMPORTANT: This is a verification run. 1. Write all suggestions to ${OUTPUT_DIR}/drift-suggestions-verify.md instead of drift-suggestions.md. 2. Do NOT write any changelog files — only write the suggestions file. 3. Before generating each suggestion, re-read the full doc section and list the 3 most important facts that the PR changes might affect. Then generate the FIND/REPLACE suggestion based on those facts.